# Build artifacts
/vibe
/bin/
/dist/
/deploy/fleetd/vibe

# Go
*.test
*.out
coverage.txt
vendor/

# Editor / OS
.idea/
.vscode/
.DS_Store
*.swp

# Local state that shouldn't be checked in
*.sock
*.pid
*.log

# Agent worktrees
.claude/

# Upstream binaries fetched by the conformance CI job / a local conformance run.
.upstream/

# scripts/check.sh pins GOLANGCI_LINT_CACHE here so the lint cache is
# per-worktree instead of per-user (see the header comment in that file).
/.cache/
