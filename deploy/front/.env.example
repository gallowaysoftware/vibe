# Copy to .env beside the compose file and fill in.

# The front's own LAN address on the macvlan network — THE fleet address
# every client is configured with.
FRONT_IPV4=REPLACE-with-a-free-LAN-IP

# Path on the Docker host to the DIRECTORY holding the llama-swap peers
# config (start from front-config.example.yaml, installed as
# <dir>/config.yaml). The whole directory is mounted so -watch-config sees
# atomic-rename (tmp+rename) writes.
FRONT_CONFIG_DIR=REPLACE-with-path-to/config-dir

# Image to run. Digest-pinned: the tag names the build, the digest is what
# docker resolves, so `docker compose pull` cannot move the front onto a
# llama-swap this repo has never gated. Leave it commented out to inherit
# the same pin from docker-compose.yaml.
#
# MOVING THE PIN IS A PROCEDURE, NOT AN EDIT: run scripts/upgrade/ritual.sh
# (canary -> gate -> fleet) and paste the digest it prints. A floating tag
# here is reported by `vibe fleet doctor` as front.image_pin.
FRONT_IMAGE=ghcr.io/mostlygeek/llama-swap:v239-cpu-b9994@sha256:6bae869ec0908538e421172fd576288e87c1bc330acde24517992507218d2c7c

# The external macvlan network's name (unraid's default is br0).
MACVLAN_NETWORK=br0

# Only needed if a cloud peer is enabled in the front config.
ANTHROPIC_API_KEY=

# Container name (optional).
FRONT_NAME=fleet-front
